"""Веб-панель Jarvis (ЧАСТЬ 6): FastAPI + одностраничный интерфейс.

Локальный интерфейс к ассистенту: чат (WebSocket), история, напоминания,
настройки инструментов (policy.yaml), логи, статус системы.
"""