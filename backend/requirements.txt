# Jarvis — новый модульный бэкенд (текстовый ассистент)
numpy>=1.26          # локальная модель классификации намерений (NumPy MLP)
requests>=2.31       # облачная LLM (JSON-план)
PyYAML>=6.0          # policy.yaml

# Веб-интерфейс (ЧАСТЬ 6): FastAPI + uvicorn (localhost-only)
fastapi>=0.110       # REST + WebSocket для web-панели
uvicorn>=0.29        # ASGI-сервер
httpx>=0.27          # клиент для тестов FastAPI (TestClient)

# D-Bus реализован на системном PyGObject (gi/Gio) — НЕ ставится через pip,
# используется системный пакет (python-gobject на Arch / python3-gi на
# Debian/Ubuntu); install.sh сам пробрасывает его в venv символьной ссылкой.

# Голосовой режим (слово-активатор, STT, TTS) — опционально:
#   pip install -r requirements-voice.txt   (или ./install.sh --with-voice)